"use strict";
export function init_service_item(e) {
    for (let t = 0; t < e.length; t++) {
        const s = e[t],
            i = document.createElement("img"),
            c = document.createElement("h2"),
            r = document.createElement("p"),
            n = document.createElement("button");
        i.setAttribute("class", "service-icon"), c.setAttribute("class", "service-title"), r.setAttribute("class", "service-description"), n.setAttribute("class", "service-more--btn"), s.appendChild(i), s.appendChild(c), s.appendChild(r), s.appendChild(n)
    }
}
const servicesImgs = [],
    servicesTitles = [],
    servicesDescriptions = [],
    servicesTarget = [],
    servicesAlts = [];
export const saveData = (e, t) => {
    servicesImgs.push(t[e].img), servicesTitles.push(t[e].title), servicesDescriptions.push(t[e].description), servicesTarget.push(t[e].target), servicesAlts.push(t[e].alt)
};
export function build_service_item(e) {
    for (let t = 0; t < e.length; t++) {
        const e = document.getElementsByClassName("service-icon"),
            s = servicesImgs.filter((e => void 0 !== e)),
            i = servicesAlts.filter((e => void 0 !== e));
        e[t].setAttribute("src", s[t]), e[t].setAttribute("alt", i[t]), console.log(servicesAlts);
        const c = document.getElementsByClassName("service-title");
        console.log(c), c[t].textContent = servicesTitles[t];
        document.getElementsByClassName("service-description")[t].textContent = servicesDescriptions[t];
        const r = document.getElementsByClassName("service-more--btn");
        console.log(r), r[t].textContent = "Več", r[t].addEventListener("click", (() => {
            window.location.href = servicesTarget[t]
        }))
    }
}