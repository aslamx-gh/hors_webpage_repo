"use strict";
let menuBtnTransition;
export function menuTriggerFunction(e, t, n) {
    clearTimeout(menuBtnTransition), "none" === e.display ? (t.classList.remove("close-menu"), t.classList.contains("open-menu") || t.classList.add("open-menu"), t.style.display = "block", menuBtnTransition = setTimeout((() => {
        n.setAttribute("src", "./img/mobile/menu-state--closing.png"), menuBtnTransition = setTimeout((() => {
            n.setAttribute("src", "./img/mobile/x-logo.png")
        }), 350)
    }), 100)) : (t.classList.remove("open-menu"), t.classList.contains("close-menu") || t.classList.add("close-menu"), setTimeout((() => {
        t.style.display = "none"
    }), 950), menuBtnTransition = setTimeout((() => {
        n.setAttribute("src", "./img/mobile/menu-state--closing.png"), menuBtnTransition = setTimeout((() => {
            n.setAttribute("src", "./img/mobile/menu.png")
        }), 350)
    }), 100)), console.log(menuBtnTransition)
}