"use strict";
import {
    saveData,
    build_service_item
} from "./../components/index_services.min.js";
export function displayServiceData(e, i) {
    for (let i = 0; i < e.length; i++) saveData(i, e);
    build_service_item(i)
}