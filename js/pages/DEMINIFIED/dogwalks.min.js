"use strict";
import {
    menuTriggerFunction
} from "../components/mobileMenuTrigger.js";
import {
    show_return_btn
} from "../components/showReturnBtn.js";
import {
    responsive_header
} from "../utils/responsive-header.js";
const header = document.getElementById("header"),
    show_menu = document.getElementById("show-menu"),
    menuTrigger = document.getElementById("menu-trigger--btn"),
    menuBtnStyle = document.getElementById("menu-btn--style"),
    head_wrapper = document.getElementById("head-wrapper"),
    MENU_FLAG = window.getComputedStyle(show_menu);
menuTrigger.addEventListener("click", (() => {
    menuTriggerFunction(MENU_FLAG, show_menu, menuBtnStyle)
})), body.addEventListener("scroll", (() => {
    show_return_btn(body, return_btn, 5)
})), responsive_header(head_wrapper, show_menu), window.addEventListener("resize", responsive_header);